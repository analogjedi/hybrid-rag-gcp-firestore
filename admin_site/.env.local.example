# =============================================================================
# Document Search Admin - Local Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# NEVER commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Google Cloud Authentication
# -----------------------------------------------------------------------------
# Path to your service account JSON key file
# Download from: GCP Console → IAM → Service Accounts → Keys → Add Key → JSON
# Required roles: Cloud Datastore User, Storage Object Admin, Vertex AI User
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# -----------------------------------------------------------------------------
# Firebase / Google Cloud Project
# -----------------------------------------------------------------------------
# Your GCP project ID (not the display name)
# Find at: https://console.cloud.google.com/home/dashboard
FIREBASE_PROJECT_ID=your-project-id

# Firestore database ID
# Use "(default)" for the default database, or your named database (e.g., "test")
# Find at: https://console.cloud.google.com/firestore
FIRESTORE_DATABASE_ID=(default)

# Cloud Storage bucket name (without gs:// prefix)
# Find at: https://console.cloud.google.com/storage
FIREBASE_STORAGE_BUCKET=your-project-documents

# -----------------------------------------------------------------------------
# Vertex AI Configuration
# -----------------------------------------------------------------------------
# Project ID for Vertex AI (usually same as FIREBASE_PROJECT_ID)
VERTEX_AI_PROJECT=your-project-id

# Vertex AI region - must have Gemini available
# Options: us-central1, us-east4, europe-west4, asia-northeast1
VERTEX_AI_LOCATION=us-central1

# -----------------------------------------------------------------------------
# AI Models (Optional - defaults shown)
# -----------------------------------------------------------------------------
# Gemini model for document analysis
# Options: gemini-2.0-flash-001, gemini-1.5-pro, gemini-1.5-flash
# GEMINI_MODEL=gemini-2.0-flash-001

# Text embedding model (768 dimensions)
# Options: text-embedding-005, text-embedding-004
# EMBEDDING_MODEL=text-embedding-005

# -----------------------------------------------------------------------------
# Cloud Functions Region (Optional)
# -----------------------------------------------------------------------------
# Region where Cloud Functions are deployed (default: us-central1)
# FUNCTIONS_REGION=us-central1

# -----------------------------------------------------------------------------
# Development Settings (Optional)
# -----------------------------------------------------------------------------
# Base URL for the app
NEXT_PUBLIC_APP_URL=http://localhost:3001
